# Koishi 群分析分阶段移植计划

目标：逐步吸收 AstrBot 上游的分析、漫画与可靠性能力，保留 Koishi / ChatLuna 的使用方式、自定义模型 API、自然语言查询和用户画像。不开发独立管理后台；提供插件配置页目录、文档与效果图链接。

## 第一阶段：预设与配置体验（已完成）

- [x] 复用 ChatLuna 动态 preset 列表与预设渲染服务；日报选择预设，漫画可继承、关闭或单独选择。
- [x] 预设消息按角色送入现有四种文本 API；每次读取最新预设。任务输出格式与漫画角色一致性约束保留，查询时间解析不受人格影响。
- [x] 配置页轻量目录：点击定位各设置分组，滚动高亮，窄屏折叠；增加参考图例、使用说明、预设说明链接。仅在本插件详情页显示。
- [x] 验证预设缺失、空预设、热更新、四种协议、漫画覆盖与禁用；完成构建和配置页交互验证。

### 第一阶段验收记录（2026-09-11）

- `npm test`：18 项通过，包含原有 API/漫画回归及新增预设测试。HTTP 使用模拟响应，未调用付费模型。
- `npm run build`：TypeScript、CJS/ESM 和配置目录前端均构建成功。
- `npm run test:ui`：Headless Edge 渲染真实目录组件和实际配置标题；验证 10 个分组、点击定位/高亮、动态标题、切换插件后的隐藏与清理、外链属性、桌面/深色/手机布局。截图保存在 `artifacts/navigation`；这是独立交互测试页，不是已部署 Koishi 实例的截图。
- 修改的后端文件 ESLint：0 错误；保留长提示词和原有 import 排序警告。
- `npm pack --dry-run`：确认 `dist/index.js`、`dist/style.css`、双模块入口和预设类型声明随包发布；实际 CJS 入口仍导出 `group-analysis`，依赖保留 Puppeteer、ChatLuna、数据库。
- 预设接入使用 ChatLuna 的消息渲染服务；不运行 Agent 的世界书检索/长期记忆/工具等会话管线，详见 README 的适用范围。真实群发送、付费模型效果和生产 Koishi 实例验收仍需部署后验证。

第一阶段基本完成；第二至四阶段已接入主要功能，但不能仅凭旧验收记录认定全部完成。2026-09-11 源码复核发现增量游标、恢复语义、名单和历史隔离等缺口，已进行下述可靠性修正。项目仍不包含独立 WebUI。

## 第二阶段：基础正确性与漫画联动

- [x] 修复单模块失败导致全部分析结果清空、用户表情比例统计错误。
- [x] 区分监听权限与定时目标，明确默认全群启用的定时发现行为；已有配置迁移不意外扩大定时发送范围。
- [x] 手动/定时日报复用同一份话题联动漫画，报告发送与生图独立；支持指定漫画时间范围。
- [x] 多角色方案、每日固定随机选择、多参考图；继续保留付费生图的防重入策略。

### 第二阶段进度（2026-09-11）

- [x] LLM 子模块使用 `Promise.allSettled`，单模块失败时保留其他结果。
- [x] 用户表情比例改为该用户表情数除以该用户消息数。
- [x] 定时日报把已生成的话题传给后台漫画任务；旧的事件载荷仍兼容。
- [x] 手动日报复用话题、漫画时间范围、权限分离和多角色方案已实现。

## 第三阶段：分析内容与报告

- [x] 聊天质量锐评（主题、维度占比、点评、总评）及各模块独立开关。
- [x] 按模块指定文本模型、重试退避、结构校验与失败隔离。
- [x] 结果持久化、无 Token 重绘、HTML 保存与可配置外链、定时多格式输出。
- [x] 主题选择、专题样式和逐主题导出验证已接入。目前保留五套原生布局；上游名称通过独立 CSS 专题层呈现，命令支持列举、切换和重绘预览。完整复刻上游模板资源仍不在本地移植范围。

## 第四阶段：高消息量与恢复

- [x] 按消息数触发增量批次、持久化游标、滑动窗口、话题/金句去重合并、全量回退。
- [x] 检查点恢复、历史任务/报告关联、Token 用量记录、全局任务/LLM/渲染并发限制。
- [x] QQ 官方文本输出已使用独立 Markdown 生成器；昵称回退和 Koishi 通用接口保留。Telegram/Discord 专用能力、QQ 官方真实发送和 OneBot 群文件/相册仍需部署验证；OneBot 归档不再套用于 QQ 官方适配器。

### 第二至四阶段验收记录（2026-09-11）

以下为修正前的历史记录，仅证明当时的构建和局部测试通过，不作为第四阶段完整验收依据。

- `npm test`：20 项通过，新增聊天质量结构校验、文本重试/模型皮肤覆盖测试。
- `npm run build`：TypeScript、CJS/ESM、配置前端构建成功。
- `npm run test:ui`：配置目录交互、动态分组和插件隔离回归通过。
- `npm run lint`：无错误，仅保留原有长提示词的 max-len 警告。
- 手动/定时分析现在写入 `chatluna_analysis_reports`，增量状态写入 `chatluna_incremental_states`，运行阶段写入 `chatluna_analysis_checkpoints`；历史、重绘和主题切换通过 Koishi 命令提供。
- 未配置 OneBot 相册扩展时，归档逻辑通过能力检测跳过；真实平台发送、HTML 外链服务器和付费 API 效果仍需部署环境验证。

### 可靠性修正与验收（2026-09-11）

- 修复启用命令处理函数误接到主题命令；主题修改要求权限 3。历史和重绘限制到当前平台、机器人和群/频道，指定报告 ID 可查询旧记录，不再只搜索最近 20 条。
- 默认全群发现后仍应用定时白/黑名单；空白名单不发现、不发送。手动指定目标群时不再沿用来源频道。
- 普通日报、自然语言日报、定时日报和增量即时报告均在渲染前启动漫画后台任务；已传话题直接复用，空话题跳过。多格式输出复用同一结果，不依赖缓存 TTL；漫画每组报告只触发一次。
- 漫画按本地日历日期固定选择角色，同一天重启保持选择稳定（角色配置不变时）；保留同群防重入、冷却，增加跨群生图并发限制。
- 增量查询使用持久化时间游标和同时间戳消息 ID 集合；待处理计数逐次保存，同群批次串行，运行中到达的消息不被清零。模型失败不推进游标，允许一次窗口全量回退；数据库历史查询先刷新缓冲，并按机器人/平台隔离。
- 增量话题/金句优先保留新结果，移除过期批次。窗口内统计重新计算；定时最终报告只在批次覆盖其完整消息时间范围时复用，否则普通全量分析。增量快照使用版本化结构，旧格式快照不作为可信游标继续使用。
- 检查点保存固定时间范围、输出格式和完整分析结果。进程中断后若已有结果则直接渲染发送，复用同一报告 ID；没有结果时重新执行该固定范围的文本分析。发送成功后才标记完成；失败结果可在当前群通过重绘命令取回。恢复不重新执行付费漫画。
- Token 统计使用任务独立上下文，兼容 Chat/Responses/Claude/Gemini 用量字段。增量批次单独保存为 incremental 分析记录；缓存重发、后续格式和增量结果输出不重复计入既有消耗。
- LLM 模块增加字段结构校验和结构错误重试；失败模块继续隔离。渲染限流覆盖页面准备、截图/PDF 和关闭，失败页面也释放。
- HTML 导出内嵌各原生皮肤的本地 CSS，避免导出目录缺少 css 子目录导致无样式；外部字体、脚本和头像仍依赖网络。分析缓存也按平台、机器人、群和频道隔离。
- QQ 官方平台使用独立 Markdown 报告生成器；其他平台仍使用通用 Koishi 消息元素。真实 QQ 官方 Markdown 投递、Telegram/Discord 专用能力和 OneBot 归档需要部署环境验证。
- 回归测试新增名单、历史隔离、漫画独立执行、游标去重、并发期间计数保留、回退、恢复、模块级检查点、Token 隔离、结构重试、多格式、平台输出和页面生命周期场景。
- 本轮最终验证：`npm test` **42/42 通过**（原 20 项，新增 22 项）；`npm run build` 成功；`npm run lint` 0 错误、6 条既有长行警告；`npm run test:ui` 通过 10 个配置分组及桌面/深色/手机交互回归；`git diff --check` 通过。构建仍有既有 Vite CJS 弃用及 import.meta 提示。
- 测试使用模拟模型、平台、数据库和页面对象验证业务故障路径；HTML 测试实际读取五套模板并写出带样式报告，配置 UI 使用 Headless Edge。未进行付费 API 调用或生产 Koishi/平台发送验收。

### 仍需完成的工作与边界

验收补修：定时增量预处理失败时继续普通日报流程，后续格式复用结果；聊天质量按批次消息量累加维度贡献，缺失维度按零贡献处理，截取前八维后统一归一化并分配舍入余数，保证总和为 100%。补充增量失败后的多格式输出、不同维度、非等权重和维度截断回归用例。

- [ ] 上游独立主题资源、布局移植及逐主题效果验收。
- [ ] QQ 官方专用输出、Telegram/Discord 专用能力、OneBot 群文件/相册真实环境测试。
- [x] 模块级中间产物断点续跑。每个成功 LLM 模块立即写入检查点；重启只重跑缺失或失败模块。发送已到达但完成状态未落库时，重启后仍可能重复投递文本/报告。
- [x] 增量称号和聊天质量的跨批次综合分析。称号按用户保留最新批次，聊天质量按消息量加权维度并重新归一化；窗口边界批次采取保守剔除，必要时回退全量。
- [ ] 大群真实负载测试。所有历史分析仍受 maxMessages 限制；超量回退不代表能无限读取聊天历史。多进程共享数据库的分布式锁不在当前实现范围。
- [ ] HTML 外链服务器和付费模型/生图的部署验收。HTML 文件保存不等于已提供公网静态服务。

## 验收原则

每阶段独立验证和交付，不把配置字段存在当作功能完成。真实 API 费用与平台实测单独记录；默认配置保留原有行为。无需为这些阶段引入独立 WebUI，后续历史记录可通过命令、HTML 外链和 Koishi 原生配置入口提供。

## 参考

- AstrBot 对照基线：`4d4e14230bb57d0fa8b47e0f9f0f6037af6d8f95`。
- [ChatLuna Agent](https://github.com/ChatLunaLab/chatluna/tree/v1-dev/packages/extension-agent)：触发器 execution.preset 与核心 preset 服务。
- [AAAAACAT](https://github.com/Sor85/AAAAACAT-chatluna-plugins)：plugin-details 插槽、plugin:name 作用域及配置标题定位方式。
